<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.stylefeng.guns.modular.system.mapper.WorkPeopleMapper">


    <select id="selectWorkPeople" resultType="map">
        select
        id, name, mobile, description, ege, zi_zhi, audit_status, audit_people, audit_time, type_name, mgr_id
        from work_people
        where 1=1
        <if test="condition != null and condition != ''">
            AND NAME like CONCAT('%',#{condition},'%')
        </if>
        <if test="typeName != null and typeName != ''">
            AND type_name = #{typeName}
        </if>
        order by id DESC
    </select>

</mapper>